<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="todolist.ListCategoryPage">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="CategoryListView" Refreshing="OnRefresh" IsPullToRefreshEnabled="True" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.ContextActions>
                            </ViewCell.ContextActions>
                            <Grid BackgroundColor="White">
                                <Label Grid.Column="0" Grid.Row="0" Text="{Binding Title}" TextColor="WhiteSmoke"/>
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>